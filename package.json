{
  "name": "twitter-clone-web",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build": "yarn --cwd ./backend && yarn --cwd ./frontend && yarn --cwd ./frontend build",
    "start": "yarn --cwd ./backend prod"
  }
}
